CREATE TABLE primaryKeys (
tableName varchar(50),
primary_key_label varchar(50),
insertedTime timestamp
)


--when a deletion occurs i need to check if the pks exist already or else just do the logic i did and then store the pks inside the table for future use

